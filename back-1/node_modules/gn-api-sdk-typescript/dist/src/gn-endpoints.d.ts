import { CredentialsInterface } from './interfaces/credentials.interface';
declare class GnEndpoints {
    private options;
    private accessToken;
    private constants;
    private defer;
    private certificate;
    private agent;
    private endpoint;
    private body;
    private params;
    constructor(options: CredentialsInterface, constants: any);
    run(name: any, params: any, body: any): Promise<any>;
    private getAccessToken;
    private req;
    private directReq;
    private directReqCallback;
    private getParams;
}
export default GnEndpoints;
